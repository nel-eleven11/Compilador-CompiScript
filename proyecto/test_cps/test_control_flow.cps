// test_control_flow.cps
// Test control flow with if statements

let a: integer = 10;
let b: integer = 5;
let max: integer = 0;

// Simple if statement to find maximum
if (a > b) {
    max = a;
} else {
    max = b;
}

// Result: max should be 10
